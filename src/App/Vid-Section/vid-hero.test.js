import React from 'react';
import { shallow, mount } from 'enzyme';

describe('Vid Hero', () => {
    it('should render the Site Title without exploding...', () => {
        const x = true;
        expect(x).toBe(true);
    })
})